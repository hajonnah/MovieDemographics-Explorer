package com.example.moviepopularitybackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main class for the Movie Popularity Backend application.
 * This class serves as the entry point for the Spring Boot application.
 */
@SpringBootApplication
public class MoviePopularityBackendApplication {
    /**
     * The main method that runs the Spring Boot application.
     * 
     * @param args Command line arguments.
     */
    public static void main(String[] args) {
        SpringApplication.run(MoviePopularityBackendApplication.class, args);
    }

}
